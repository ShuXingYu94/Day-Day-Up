import itertools

n = int(input('Input n : '))
a = n * (n - 1)
print(a)
nlist = []
for i in range(1, n + 1):
    nlist.append(i)
b = itertools.permutations(nlist, n)
for t in list(b):
    print(' '.join(map(str, list(t))))
