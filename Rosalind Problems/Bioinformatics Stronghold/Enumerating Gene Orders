import math
import itertools

n=5
print(math.factorial(n))
nlist = []
for i in range(1, n + 1):
    nlist.append(i)
b = itertools.permutations(nlist, n)
for t in list(b):
    print(' '.join(map(str, list(t))))
